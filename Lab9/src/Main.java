package Lab9.src;

import java.io.BufferedReader;
import java.io.FileNotFoundException;
import java.io.IOException;
import java.util.*;
import java.io.InputStreamReader;

public class Main {
    public static void main(String[] args) throws FileNotFoundException, IOException {

        // Part 1 Word Search
        /*
         * Scanner sc = new Scanner(System.in);
         * System.out.println("Enter the file path of the puzzle file: ");
         * String puzzle = sc.nextLine();
         * System.out.println("Enter the file path of the word list file: ");
         * String wordList = sc.nextLine();
         * sc.close();
         * WordSearch test = new WordSearch(puzzle, wordList);
         */

        // Part 2 Video Game Scoreboard
        // Scanner was having issues again so I went with a different I/O class
        /*
         * int userInput = 1;
         * Scoreboard board = new Scoreboard();
         * BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));
         * while(userInput == 1) {
         * System.out.println("Enter a name for a high score: ");
         * String name = reader.readLine();
         * System.out.println("Enter the score for that name: ");
         * int score = Integer.parseInt(reader.readLine());
         * board.insert(name, score);
         * 
         * System.out.
         * println("Enter 1 to add another score or 0 to stop adding scores: ");
         * userInput = Integer.parseInt(reader.readLine());
         * }
         * board.printScores();
         */
    }
}
